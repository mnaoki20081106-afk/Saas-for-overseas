# DESIGN_REVIEW — この設計への自己批判と、それによる修正

> 設計書を書いたあと、実装を始める前に、自分の設計を疑いました。
> **14の問いに対して、正直に答え、問題が見つかったものは設計を修正しました。**
> 修正した箇所には ✅ **修正済み** と書いてあります。

---

## ⚠️ 最初に — この設計への最大の批判

**「AI会社を作ること」は、いま最も収益に近い作業ではありません。**

いまの状態を正直に整理します。

| 要素 | 状態 |
| --- | --- |
| 記事を作る仕組み | **完成している**（5,801行・品質ゲート・自動修正まで実装済み） |
| ピンを作る仕組み | **完成している**（画像描画・予約・横展開まで実装済み） |
| 投稿する仕組み | **完成している**（Pinterest API v5 クライアント実装済み） |
| **アフィリエイトリンク** | **0件**（`config/affiliate-links.json` が空） |
| **Pinterest の投稿権限** | **なし**（Trial access のまま。作ったピンは自分にしか見えない） |
| **本物の記事** | **0本**（すべて DRY_RUN のサンプル） |

つまり **エンジンは完成しているのに、燃料が1滴も入っていません。**

この状態で AI会社化（＝エンジンをより賢いエンジンに載せ替える作業）を先にやると、
**「もっと賢い、しかし燃料の入っていないエンジン」ができるだけです。**

### したがって推奨する順序

```
Phase 0（止血・30分）
   ↓
【並行】
  A: なおきさんが人間タスクを3つ進める        ← これが収益の律速段階
     ①Anthropic は不要になったので飛ばす
     ②Pinterest Standard access の審査申請（画面録画の提出）
     ③アフィリエイト 3件に応募（応募文は下書き済み）
  B: 私が Phase 1（AI会社の基盤）を作る
   ↓
両方そろった時点で、本物の案件で最小ループを1周させる
```

**A と B は並行できます。** A は審査待ちで数日〜数週間かかるので、待っている間に B を作るのが最適です。
ただし **「B が終われば収益が出る」わけではない**ことは、はっきりさせておきます。

---

## 1. 本当に Claude API なしでできるか？

**答え：できる。ただし1つ、設計を修正した。**

| 工程 | Claude API なしで可能か | 代替手段 |
| --- | --- | --- |
| Web検索によるリサーチ | ✅ | Claude Code の WebSearch / WebFetch |
| 記事の設計・執筆 | ✅ | Writer skill |
| 検品 | ✅ | サブエージェント（**むしろ独立性が上がる**） |
| ピン文案 | ✅ | Designer skill |
| **JSON の形の保証** | ⚠️ **API の機能では代替できない** | zod による検証コマンド（`co ...:submit`） |
| 画像生成 | ✅ | 既存の Chromium 描画（もともと $0） |

### ✅ 修正済み：`SessionHandoff` の設計を捨てた

当初 MIGRATION.md に、既存の `withFixture()` を3分岐に拡張して
`SessionHandoff` 例外を投げる、という設計を書きました。**これは間違いです。**

理由：既存の `writeOneArticle()` は「設計 → 執筆 → レビュー → 品質ゲート → 保存」を
1つの関数の中で連続実行します。途中で例外を投げてセッションに制御を渡すと、
**関数の途中の状態（brief は作ったが本文はない）をどこにも保存できません。**

**修正後の設計：**

- セッションモードでは **既存の `writeOneArticle()` を呼ばない。**
- 代わりに `co` CLI が**工程ごとに独立したコマンド**を提供し、skill がそれを順に呼ぶ。

```
co writer:context <ideaId>   → 執筆に必要な情報を全部まとめて標準出力に出す（AI が読む）
                                （AI が content/drafts/<slug>.md を書く）
co writer:check <slug>       → 品質ゲートを実行して、不合格項目を列挙する
                                （AI が直して再度 check）
co writer:submit <slug>      → 合格していれば articles.json に登録し、Editor のタスクを作る
```

- `AI_BACKEND=api` のときは **既存の `writeOneArticle()` がそのまま動く。**
  つまり**既存コードには一切手を入れず、新しい経路を横に足す**形になります。
  これなら「既存機能を壊さない」を確実に守れます。

---

## 2. Claude Code on the web の機能を過大評価していないか？

**答え：3点、過大評価していた。修正した。**

| 当初の想定 | 事実 | 修正 |
| --- | --- | --- |
| ルーチンから `main` に直接 push できる | `claude/` 接頭辞のブランチには常に push できる。他ブランチは保護状態などをチェックされる | ✅ **`claude/autopilot-<日付>` に push → `guard.yml` が検査 → 自動マージ** に変更 |
| ルーチンは好きな頻度で回せる | **最小間隔は1時間**。それより短い cron は拒否される | ✅ 1日1〜3本の設計なので影響なし。ただし「3時間おきのピン投稿」は **Actions に残す**（元々そう設計していた） |
| ルーチンの環境変数に秘密情報を置ける | **その環境を使う人全員から見える** | ✅ **秘密情報は Actions Secrets のみ**（元々そう設計していた。SECURITY.md §3 に明記） |
| ルーチンの実行回数は無制限 | **アカウント単位の日次上限がある**（数値は claude.ai/settings/usage で確認） | ✅ MVP は1日1本。実測してから増やす |

### ✅ 修正済み：ネットワークポリシーを `Full` にすると明記した

当初「Custom で必要なドメインだけ許可」と書きましたが、**Researcher は「まだ知らない SaaS の
公式サイト」を読むので、ドメインを事前に列挙できません。** 実質 `Full` が必要です。

これは受け入れます。理由：**この環境には秘密情報がなく、リポジトリも public なので、
外に出て困るものがありません。** SECURITY.md §3.3 に明記しました。

### さらに1点：ルーチンの作成は claude.ai/code/routines から行う

`/schedule` コマンドは **Claude Code のセッションの中では使えません**（Web UI から管理する）。
なおきさんは iPad の Safari で claude.ai/code/routines を開いてフォームを埋めることになります。

✅ **修正済み：** Phase 1 の人間タスクに「ルーチンを作る」を追加し、
**フォームに貼り付ける文章をそのまま用意する**ことにしました（→ §11）。

---

## 3. Claude Pro の利用制限に引っかからないか？

**答え：引っかかる可能性がある。だから MVP を1日1本にした。**

事実：

- Routines は通常のサブスク利用枠を消費する
- その枠は claude.ai のチャット / Claude Code / Desktop と**共有**
- さらに Routines には**アカウント単位の日次実行上限**がある

つまり **AI会社が動く分だけ、なおきさん自身が Claude を使える量が減ります。**

✅ **修正済み：**

- MVP は **1日1本のルーチン**から始める（当初は朝・昼・夜の3本を想定していた）
- `data/kpis.json` に1日のルーチン実行回数を記録し、上限を超えたら自動停止
- Phase 2 で1週間の実測を取ってから本数を決める
- 足りない場合の選択肢（本数を減らす / Max に上げる / ピン文案だけ API に戻す）を COSTS.md §4 に明記

**「Pro で全部いけます」とは断言しません。実測してから判断します。**

---

## 4. GitHub Actions を残すべき部分はないか？

**答え：ある。むしろ Actions が担うべき仕事のほうが多い。**

Actions に残すべき条件は明確です：**①秘密情報が要る ②分単位の周期が要る ③判断が要らない**

| 処理 | Actions に残すか | 理由 |
| --- | --- | --- |
| Pinterest 投稿 | **残す** | ①③。トークンのリフレッシュが必要で AI に渡せない |
| 数値取得 | **残す** | ①③ |
| ピン画像の描画 | **残す** | ③。Chromium が要る。判断は不要 |
| サイト生成・Pages 公開 | **残す** | ③ |
| 安全装置の検査（`guard.yml`） | **残す** | ③。**AI が触れない場所に置く必要がある** |
| CI（typecheck / DRY_RUN） | **残す** | ③ |
| Pinterest トークン交換 | **残す** | ① |
| リサーチ・執筆・検品・分析 | **Routine へ** | 判断が要る |

**この分割で、Actions は「秘密の金庫と実行係」、Routine は「頭脳」になります。**

なお、public リポジトリなので **Actions の実行時間は無制限・無料**です。
Actions を減らす動機はまったくありません。

---

## 5. 無料でできる処理に有料 API を使っていないか？

**答え：使っていない。むしろ既存実装が既に正しい判断をしている。**

| 処理 | よくある有料の選択 | この設計の選択 | 差額 |
| --- | --- | --- | --- |
| ピン画像 | 画像生成 API | **HTML/CSS + Chromium** | 月 $50〜150 の節約 |
| キーワード調査 | Ahrefs / Semrush | **競合記事の実測** | 月 $99〜 の節約 |
| データベース | Supabase / Firebase | **git 上の JSON** | 月 $0〜25 の節約 |
| ホスティング | Vercel / Netlify | **GitHub Pages** | 月 $0〜20 の節約 |
| 予約投稿 | Tailwind | **Pinterest API + Actions** | 月 $15 の節約 |
| 定期実行 | サーバー / Lambda | **Routines + Actions** | 月 $5〜20 の節約 |

**既存実装の「ピン画像を Chromium で撮る」という判断は、この事業で最も価値のある設計判断です。**
ここは絶対に触りません。

---

## 6. AI社員を増やしすぎていないか？

**答え：MVP では増やしすぎていた。5人に減らした。**

当初 Phase 1 から8人全員を実装する前提で書いていました。**これは間違いです。**

理由：**Analyst と Growth は「過去のデータから判断する」職能ですが、
Phase 1 の時点で過去のデータは1件もありません。** 分析する対象がないのに分析役を作るのは無駄です。

✅ **修正済み：段階的に社員を増やす**

| Phase | 稼働する社員 | 理由 |
| --- | --- | --- |
| **Phase 1（MVP）** | **CEO / Researcher / Writer / Editor / Designer の5人** | 1周させるのに最低限必要な役 |
| Phase 2 | + **QA** | 記事が増えて機械チェックだけでは足りなくなる |
| Phase 3 | + **Analyst / Growth** | 数字が入り始めて、初めて分析対象ができる |

Phase 1 では：
- **Analyst の仕事（企画）は CEO が兼務**（案件が1〜3件しかないので選択肢が少ない）
- **QA の14項目のうち、自動化できる8項目は `co qa:check` がコードで実行**。
  AI の QA は Phase 2 から
- **Growth の仕事は存在しない**（投稿実績がゼロなので分析できない）

**結果：Phase 1 で起動するサブエージェントは1ルーチンあたり最大3つ（Writer / Editor / Designer）。**
これならコンテキストも利用枠も現実的です。

---

## 7. AI社員同士の責任範囲は明確か？

**答え：曖昧な箇所が7つあったので、AGENTS.md §5 に判定表を作った。**

特に危なかったのは次の2つです。

| 曖昧だった点 | 明確化 |
| --- | --- |
| 「記事を公開してよいか」を QA が決めるのか CEO が決めるのか | **QA は品質の合否のみ。公開の可否は人間の GO** |
| 「ピンの画像」を Designer が作るのか | **作らない。Actions が描画する。** Designer はテンプレIDと文言だけ |

もう1つ、追加で見つかった曖昧さがあります。

### ✅ 修正済み：Editor と QA の検品範囲が重複していた

当初、Editor の11項目と QA の14項目に「事実誤認」「リンク切れ」が両方入っていました。
二重に検品すると、**片方が「もう片方が見ているはず」と考えて手を抜くリスク**があります。

**修正後の分担：**

| 担当 | 見るもの | ひとことで言うと |
| --- | --- | --- |
| **Editor** | 文章そのもの（英語の自然さ・文法・語彙・誇張・重複・読みやすさ） | **「読み物として自然か」** |
| **QA** | 文章の外側（数値と出典の一致・リンクの生存・メタデータ・画像・開示・規約） | **「間違っていないか」** |

事実確認は **QA が担当**（出典と照合するのは機械的作業に近い）。
Editor は「その主張は言い過ぎでは？」という**文章上の違和感**だけを見ます。

---

## 8. 無限ループしないか？

**答え：しない。ただし1箇所、抜け穴があったので塞いだ。**

対策は SECURITY.md §4 T5 のとおりですが、レビューで見つけた抜け穴：

### ✅ 修正済み：Editor → Writer の修正ループが無限になりうる

当初の設計では「Editor が不合格 → Writer が直す → Editor が再検品」というループがありました。
`maxEditorRounds: 2` を設定していましたが、**このカウンタをどこに保存するかを決めていませんでした。**

セッションが落ちて再実行されると、カウンタがリセットされて永久にループします。

**修正後：** `reviews.json` の `round` フィールドを唯一の真実とし、
`co editor:submit` が「同じ `targetRef` の既存レビュー数 + 1」を自動で採番します。
**AI が round を自己申告する余地をなくします。** 3回目に到達したら自動で `needs_human`。

同じ問題が `tasks.attempts` にもあったので、**`co` が採番する**方式に統一しました。

---

## 9. データが蓄積されるか？

**答え：される。ただし「捨てるデータ」も決めた。**

蓄積されるもの：25のエンティティ（→ DATA_MODEL.md）。すべて git に残るので履歴も追える。

### レビューで気づいた点：ファイルが無限に太る

| ファイル | 増え方 | 対策 |
| --- | --- | --- |
| `runlog.json` | 実行のたびに1件 | 既存実装が **200件で切り捨て**（正しい） |
| `metrics.json` の `history` | 日次1件 | 既存実装が **400件で切り捨て**（正しい） |
| `kpis.json` | 日次1件 | ✅ **修正済み：730件（2年）で年次アーカイブへ** |
| `decisions.json` | 実行のたびに数件 | ✅ **修正済み：1,000件で年次アーカイブへ。ただし `outcome` が埋まっているものは残す**（学習資産） |
| `errors.json` | 失敗のたび | ✅ **修正済み：`handled: true` のものは90日で削除** |
| `pins.json` | ピンのたび | 5,000件で年次アーカイブ（**2年以上先**） |
| `research.json` | リサーチのたび | ✅ **修正済み：`verdict: rejected` は理由だけ残して詳細を削る（再調査の無駄を防ぐため理由は残す）** |

---

## 10. 過去の結果を次の意思決定に利用できるか？

**答え：できる。ただし「過去がない最初の3ヶ月」の設計が抜けていた。**

これはレビューで見つけた**最も重要な欠陥**です。

`ideas.json` の `basedOn` フィールドは「過去のどのデータから判断したか」を必須にしていますが、
**Phase 1〜2 には過去のデータが1件もありません。** このままだと AI は
「データがないので判断できません」と止まるか、**もっと悪いことに、根拠を捏造します。**

### ✅ 修正済み：コールドスタート方針を明文化

**最初の10本の記事と最初の100枚のピンは、「最適化」ではなく「データを作る」ために出します。**

```
記事タイプ  : 4種類（比較 / 代替 / 課題別 / 単体レビュー）を機械的に順番に回す
              → 既存の ARTICLE_TYPES ローテーション実装をそのまま使う
テンプレート: 5種類を各記事で必ず全部使う（既存の pinPrompt が既にそう指示している）
配色        : 8種類を順番に回す（既存の paletteIndex 実装をそのまま使う）
切り口      : 10種類を各記事で必ず全部使う
投稿時刻    : 既存の postingHoursUtc を均等に使う
```

**この期間、AI は「どれが良いか」を判断してはいけません。**
`basedOn.confidence` は必ず `"low"`、`sampleSize` は実際の値（0 や 3）を正直に書き、
**CEO は承認カードに「まだ根拠となるデータがありません。データを作るための1本です」と明記します。**

母数が閾値（ピン20枚 × 300 impressions）を超えた時点で、初めて Analyst が判断を始めます。

**これがないと「根拠がないのに、あるふりをする AI」ができあがります。**
既存の `docs/RUNBOOK.md` に書かれている「300表示以上を集めたピンが20枚以上たまってから判断してよい」
という記述は正しく、それをデータ構造のレベルで強制する形にしました。

---

## 11. 人間に専門知識を要求していないか？

**答え：3箇所で要求していた。2つは直せる。1つは直せない。**

| # | 要求していたもの | 判定 | 対応 |
| --- | --- | --- | --- |
| 1 | `npm run autopilot link:set <slug> <url>` をターミナルで実行 | ❌ **要求してはいけない** | ✅ **修正済み：管理画面にリンク登録フォームを追加**（既存の admin/page.ts に既にその枠組みがある） |
| 2 | Routine をフォームから作る | △ | ✅ **修正済み：貼り付けるだけの文面を用意する**（下記） |
| 3 | Pinterest Standard access のための OAuth 画面録画 | ❌ **これは直せない** | 手順書を書く。iPad の画面収録機能で撮れる |
| 4 | GitHub の Secrets 登録 | △ | 既存の `TODO-HUMAN.md` に手順あり。管理画面からもリンクする |
| 5 | アフィリエイト応募フォームの記入 | ❌ 直せない（本人確認・契約行為） | 回答文は AI が全部下書き済み。コピペのみ |

### ✅ 修正済み：Routine 作成の「貼り付ける文面」を Phase 1 の成果物にする

なおきさんが claude.ai/code/routines で **New routine** を押したあと、
フォームの各欄に何を入れるかを、**そのままコピペできる形**で用意します。

```
名前:
  AI会社の朝礼

リポジトリ:
  mnaoki20081106-afk/Saas-for-overseas

環境:
  saas-autopilot（Network access: Full / 環境変数 AI_BACKEND=session）

スケジュール:
  毎日 07:00（日本時間）

プロンプト（この枠に丸ごと貼る）:
  あなたはこの会社の CEO です。
  .claude/skills/ceo/SKILL.md の手順に厳密に従って、今日の業務を実行してください。
  手順を省略しないでください。最後に必ず commit と push を行ってください。

コネクタ:
  （全部外す）
```

**「プロンプトは1行、あとは全部リポジトリの中の手順書が決める」構造にします。**
そうすればプロンプトを直したいときも、なおきさんが Routine を触る必要がありません（リポジトリを直すだけ）。

---

## 12. iPad だけで管理できるか？

**答え：できる。ただし §11 の修正が前提。**

| 操作 | iPad でできるか | 手段 |
| --- | --- | --- |
| 会社の状態を見る | ✅ | `/admin/`（GitHub Pages） |
| 承認する（GO / STOP） | ✅ | `/admin/` のボタン |
| 全部止める | ✅ | `/admin/` の赤いボタン |
| アフィリエイトリンクを登録 | ✅ **（修正で対応）** | `/admin/` のフォーム |
| ルーチンを作る・止める | ✅ | claude.ai/code/routines（Safari） |
| 失敗の原因を見る | ✅ | `/admin/` の「最近の失敗」 |
| 失敗の相談 | ✅ | `/admin/` から Claude Code のセッションを開く導線 |
| Pinterest OAuth | ✅ | 既存の `/pinterest-connect/` + Actions（ターミナル不要・実装済み） |
| Pinterest の画面録画 | ✅ | iPad の画面収録 |
| GitHub Secrets の登録 | ✅ | GitHub の Web UI（Safari） |
| アフィリエイト応募 | ✅ | 各社の Web フォーム |

**ターミナルが必要な操作は1つもなくなります。**

---

## 13. 外部サービスの API 仕様と現在の実装は一致しているか？

**答え：Pinterest は一致。アフィリエイト3社に既知の弱点がある。**

| 実装 | 判定 | 見つかった問題 |
| --- | --- | --- |
| Pinterest OAuth（refresh_token → access_token） | ✅ 一致 | — |
| Pinterest `POST /pins` | ✅ 一致 | **ただし Trial access では 403（実際に発生中）** |
| Pinterest `GET /boards`（bookmark ページング） | ✅ 一致 | — |
| Pinterest `GET /pins/{id}/analytics` | ⚠️ | レスポンスを `res.ALL ?? Object.values(res)[0]` で読んでいる。実データで確認していない。**Standard access が通った直後に実測で確認すべき** |
| Impact `GET /Actions` | ⚠️ | `PageSize=1000` を指定しているが**ページングしていない**。成果が1,000件を超えると欠落する（当面は問題にならないが、成功したら壊れる） |
| ShareASale 署名 | ⚠️ | 署名方式は実装されているが、実キーで検証していない |
| PartnerStack `GET /transactions` | ⚠️ | **日付でフィルタするクエリを送らず、取得後にクライアント側で絞っている。** `limit=500` を超えると古いデータしか取れない可能性がある |

✅ **修正済み：** これらを Phase 3 の作業項目に追加しました。
**「収益が出始めたときに壊れる」タイプのバグ**なので、成果が出る前に直す必要があります。

なお **どれもパイプライン全体を止めない設計**（取れなければ警告して 0件を返す）になっており、
その判断は正しいです。

---

## 14. 本当に利益につながる構造になっているか？

**答え：構造としては正しい。ただし律速段階はここではない。**

### 構造の評価

| 要素 | 評価 |
| --- | --- |
| 継続報酬型に絞る | ✅ **正しい。** 1件の成約が14ヶ月効き続ける。積み上がる |
| 日本語競合の薄いニッチを英語で攻める | ✅ **合理的。** ただし英語圏の競合は普通にいるので、`englishCompetition` も見る既存実装は正しい |
| Pinterest を主導線にする | ✅ **正しい。** 新規ドメインで Google の評価を待つより速い |
| ストック型記事（年号・「最新」の禁止） | ✅ **非常に正しい。** 書いた記事が資産になる |
| ピン画像を $0 で作る | ✅ **この事業の勝ち筋。** 変動費がほぼゼロなので、量が正義になる |
| 勝ち型の横展開 | ✅ **正しい。** ただし母数が貯まるまで判定してはいけない |

### しかし、収益の律速段階は別のところにある

```
記事を書く力         ← 完成している（AI会社化でさらに良くなる）
     ↓
Pinterest に投稿する ← ⛔ Trial access で止まっている
     ↓
記事を読ませる       ← 上が詰まっているので 0
     ↓
リンクをクリックさせる← ⛔ アフィリエイトリンクが0件なので、クリックされても収益0
     ↓
成約させる           ← 0
```

**上流（記事生成）をどれだけ賢くしても、下流の2つの詰まりが解けなければ収益は 0 のままです。**

### したがって、この設計の価値の正直な評価

| AI会社化がもたらすもの | 収益への直接の効果 |
| --- | --- |
| 記事の品質が上がる（検品の独立性） | **中期的に効く**（詰まりが解けた後） |
| 判断が状況適応的になる | **中期的に効く** |
| Claude API 課金が消える | **月 $6〜22 の削減**（小さい） |
| なおきさんの作業が GO だけになる | **継続可能性が上がる**（これが最大の価値） |
| Pinterest の投稿権限が得られる | **❌ もたらさない**（人間の審査申請が必要） |
| アフィリエイト承認が得られる | **❌ もたらさない**（人間の応募が必要） |

**「AI会社を作れば収益が出る」わけではありません。**
AI会社は「詰まりが解けたあと、人手をかけずに走り続けるための仕組み」です。

だから §冒頭のとおり、**A（人間タスク）と B（AI会社の実装）を並行**するのが正解です。

---

## 追加で見つかった問題（14の問い以外）

### 15. ✅ 修正済み：承認が溜まると会社が止まる

なおきさんが3日間 iPad を見なければ、承認待ちが溜まって何も進まなくなります。

**修正：**
- 承認が必要なのは **外に出る行為だけ**。調査・企画・執筆・検品・ピン文案作成は**承認不要**で進む
- したがって承認が止まっても、**「公開待ちの完成品」が積み上がるだけ**で、会社は仕事を続ける
- 承認は72時間で期限切れ → 実行されない（勝手に実行はしない）
- 同時に `pending` にできるのは3件まで → 画面が溢れない

### 16. ✅ 修正済み：Designer が同じ記事に何度もピンを作りうる

既存の `optimize.ts` は勝ち型を「別カテゴリの記事」へ横展開しますが、
記事が2〜3本しかない Phase 1〜2 では、同じ記事にピンが集中します。

**修正：** `co designer:submit` が「1記事あたりの累計ピン数」を上限（既定30枚）でチェックします。
既存実装の `usedTargets` による重複回避は正しいので、そこに上限を足す形にします。

### 17. ✅ 修正済み：`content/drafts/` が `.gitignore` に入っていない

下書きは公開されないべきですが、`public/` を作るのは `site/build.ts` であり、
`content/drafts/` を読まないので**実際には公開されません**。ただし混乱を避けるため、
`site/build.ts` に「`content/articles/` のみを読む」ことを明示的にコメントで書きます。
（gitignore には**入れません**。下書きも履歴に残すべき資産です。）

### 18. 未解決：Pinterest アカウントが停止したときの代替経路

`pins:export` で手動投稿へ退避できますが、**アカウント自体が停止したら手動投稿もできません。**

**現時点の対策：** 停止確率を下げる（ランプアップ・上限・重複禁止・投稿間隔）ことに集中します。
代替チャネル（X / Reddit / 検索流入）への分散は Phase 5 の候補として残します。

**これは残るリスクとして受け入れます。** 分散は工数がかかり、
1つのチャネルすら立ち上がっていない段階でやることではありません。

---

## レビューの結果、設計に加えた修正の一覧

| # | 修正内容 | 影響した文書 |
| --- | --- | --- |
| 1 | `SessionHandoff` 方式をやめ、工程ごとの独立コマンド方式に変更 | MIGRATION.md |
| 2 | ルーチンは `claude/` ブランチに push → `guard.yml` 検査 → 自動マージ | ARCHITECTURE.md / SECURITY.md |
| 3 | ネットワークポリシーは `Full` が必要と明記 | SECURITY.md |
| 4 | MVP のルーチンを1日1本に（当初3本） | ARCHITECTURE.md / COSTS.md |
| 5 | **MVP の AI社員を5人に削減**（Analyst / Growth / QA は後のフェーズ） | AGENTS.md / ROADMAP.md |
| 6 | Editor と QA の検品範囲の重複を解消（文章 vs 事実） | AGENTS.md |
| 7 | `round` と `attempts` は AI ではなく `co` が採番する | AGENTS.md / DATA_MODEL.md |
| 8 | データファイルのアーカイブ方針を追加 | DATA_MODEL.md |
| 9 | **コールドスタート方針を明文化**（最初はデータを作るために出す） | AGENTS.md / DATA_MODEL.md |
| 10 | アフィリエイトリンク登録を管理画面に（ターミナル排除） | ROADMAP.md |
| 11 | Routine 作成の貼り付け文面を成果物に追加 | ROADMAP.md |
| 12 | アフィリエイト3社の API 実装の弱点を Phase 3 に追加 | ROADMAP.md |
| 13 | 1記事あたりの累計ピン数の上限を追加 | DATA_MODEL.md |
| 14 | **推奨する実行順序を「人間タスクと並行」に変更** | この文書の冒頭 |

---

## 最後に — この設計を採用しない場合の選択肢

正直に、代案も書いておきます。

| 選択肢 | 内容 | 向いている場合 |
| --- | --- | --- |
| **A. この設計を実装する** | AI会社化 + Claude API 廃止 | 長く続けたい。毎日の指示を出したくない |
| **B. Phase 0 だけやって、既存のまま API キーを入れる** | サンプル記事を止めて、本物の記事を生成し始める | **今すぐ本物の記事が欲しい。**月 $6〜22 は払える |
| **C. 何も作らず、人間タスクだけ進める** | Pinterest 審査 + アフィリエイト応募に集中 | 収益への最短距離だけを見る |

**私の推奨は「Phase 0 → C と A を並行」です。**

B も悪くありません。**「今日から本物の記事を積み始めたい」なら、
API キーを1つ入れるだけで今すぐ動きます。**月 $6〜22 で3ヶ月分の記事が積めます。
A の実装には時間がかかるので、その間に B で記事を積むという判断もあり得ます。

どれを選ぶかは、なおきさんの判断です。

---

## 関連文書

- [ARCHITECTURE.md](ARCHITECTURE.md) / [AGENTS.md](AGENTS.md) / [DATA_MODEL.md](DATA_MODEL.md)
- [MIGRATION.md](MIGRATION.md) / [COSTS.md](COSTS.md) / [SECURITY.md](SECURITY.md) / [ROADMAP.md](ROADMAP.md)
